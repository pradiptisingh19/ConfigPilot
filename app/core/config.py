"""Application-wide configuration."""

APP_NAME = "ConfigPilot"
APP_VERSION = "0.1.0"
APP_DESCRIPTION = "Feature Flag and Remote Configuration Service"

# SQLite stores the database in a single file in the project root
DATABASE_URL = "sqlite:///./configpilot.db"
